# Codex implementation prompt — Interiorin

Work in `E:/Personal Project/Interiorin`, a Next.js 16 / React 19 application using React Three Fiber/Three, Radix Dialog, Lucide, Motion, Zustand, Zod, and the existing prepared spatial proof. Preserve user changes and Git history. Baseline source commit is `b1b392f`; do not reset, overwrite unrelated work, add secrets, claim a host, or edit Forge run state. Inspect current code before implementation and reuse existing semantic, provider, proof, transaction, receipt, and scene primitives.

## Product and release boundary

Interiorin is an evidence-aware residential studio spanning exactly three flows: evidence→baseline; reasoned options→bounded refinement; named comparison→scoped handoff. The complete objective includes real empty/existing interior/exterior intake, honest 3D, reasoned options, ElevenLabs voice, Gemini/Nano Banana derivatives, immutable versions, compare, and professional handoff. These are **design horizon**, not current release claims. Every horizon route visibly says `Design horizon · not in this release` and must remain feature-gated until its data/provider/persistence contract exists.

The only current executable route is `/proof/prepared-dining-room`, labelled `Verified release slice · prepared interior proof`, local production/NO_HOST, prepared fallback, session-only, `unreviewed`. Keep the existing no-live-provider disclosure. Never claim real capture, exterior runtime, persistence, voice/image execution, professional verification, certification, survey/buildability, mobile release, hosted runtime, live Terra, or moat.

## Architecture and object boundaries

Maintain this model: Project → Space(interior/exterior, empty/existing) → EvidenceSource → Fact(value/unit/authority/freshness/source) → CanonicalBaseline → ObjectOrZone → Policy → frozen TypedAction → DesignOption → immutable Version → Receipt → PresentationDerivative → HandoffPackage → scoped ReviewResponse. Generated imagery cannot mutate facts. Voice cannot grant authority. Direct manipulation, keyboard, touch, text, and voice must all produce the same typed `propose → inspect → preview → confirm → commit → receipt` contract. Undo creates a reversing Version.

Routes: `/projects`, `/projects/:projectId/evidence/:spaceId`, `/studio/:optionId`, `/proof/prepared-dining-room`, `/compare?a=&b=`, `/versions?from=studio|compare`, `/handoff/:packageId`, and an external signed reviewer route. Visible controls: New project, Open prepared proof, Version history, Back to Studio/Compare, Return to Interiorin project, Close review. Preserve filter/draft/selection/scroll in history; route activation focuses H1 and announces; Back restores state/trigger focus.

Implement 11 route/screen modules matching the standalone design contract: F1.1 Project Folio, F1.2 Evidence Mission, F1.3 Baseline Review, F2.1 Option Studio, F2.2 Option Workbench, F2.3 Derivative Plate, F2.R Prepared Proof, F3.1 Compare, F3.2 Version Ledger, F3.3 Handoff Builder, F3.4 Reviewer Package. Horizon modules may be honest disabled/read-only shells only when backed by explicit status and must never appear functional without real state.

## Critical F1.2 data contract

Use task states not_started/active/uploading/processing/complete/skipped(reason)/error/needs_review. Reopen invalidates dependents. Interior tasks I00 permissions/privacy, I10 frame, I20 boundaries, I30 openings, I40 retained objects, I50 scale anchor, I60 limits. Exterior E00 permissions/privacy, E10 zone, E20 boundary, E30 grade/drainage, E40 utilities/structures, E50 setbacks/jurisdiction, E60 environment, E70 scale, E80 limits. Validate integer ranges: walls 300–30,000 mm; ceiling 1,800–10,000; openings width 100–10,000/height 100–5,000; objects 10–10,000; anchor 10–30,000. Accept images ≤20 MB/40, video ≤250 MB/5, PDF ≤50 MB/10; reject MIME mismatch, zero-byte, protected, or failed checks. Review enables only when required tasks complete, ≥1 source exists, fit missions have scale, nothing required is processing/error/needs_review, and limits are acknowledged.

## Canonical F2.R contract

Do not change the causal order or leak any equivalent of `18 cm`, `180 mm`, `+18`, `+180`, `0.18 m`, `1100 mm`, `1.1 m` before proof-valid Pass B—not in DOM, accessible names/descriptions/live regions, Canvas props/labels, ghosts, animation setup, logs exposed to UI, or disabled controls.

Render exactly six SHA-256 rows over UTF-8 sorted canonical JSON with integer mm: Geometry Pass A/B, Transaction Pass A/B, Authority Pass A/B. Each row state is only Pending/Hashed/Error, displays first 12 hex characters and a 44 px Copy full digest control. Separately compute Geometry MATCH/MISMATCH, Transaction MATCH/MISMATCH, Authority 1 FIELD/MISMATCH. Authority passes only when bookcase width authority/sourceEventId is the sole changed tuple field and value remains 1,000 mm. Require six Hashed rows and three passing relationships before solver.

Receipt order: version/time zone; requested +400; committed +180; outcome; policy ID/version/full hash; Prepared fallback/no model request; attestation event/time; five bases in literal fact-ID order; six full wrapping proof hashes; three relationship summaries; unreviewed; table x 920→1100; limitation. Full receipt hashes never ellipsize.

R3F: room 5200×4000×2700 mm; table 1600×900×750 at x920,z2000 using material.oak; bookcase 1000×350×1800 at x3300,z350 using material.bookcloth; initial clearance 1080 and post-move 900. Camera fov38, near .1/far50, position `[4.8,4.2,6.8]`, target `[2.6,.75,2]`; hemisphere .9, directional 1.6 at `[4,7,5]`, contact shadow .12. Selected stroke 3 px. Ghost declared opacity `.28` only after valid Pass B. Orbit polar 35–78°, azimuth ±55°, distance 5.2–8.8; no pan/auto-orbit. Provide labelled 44 px rotate/zoom/reset/semantic controls and keyboard equivalents. Canvas failure leaves the entire semantic proof operable.

## UI implementation rules

Implement Material Ledger Studio directly with canvas/surface/model `#F4F0E7/#FBFAF6/#E8E0D3`, ink/graphite `#202923/#5A625C`, charcoal/inverse `#252824/#FBFAF6`, verified/declared/observed/inferred/danger `#35624F/#2F596A/#9B4835/#7A5810/#8C3028`, essential boundary `#4D554F`, decorative rules `#A79E91/#D8D0C3`, oak/bookcloth `#8A6E52/#6F5B47`. Newsreader/Georgia display/H1/H2 is 56/40/28 px; IBM Plex Sans/Arial H3/body/small/meta is 20/16/14/12; Plex Mono/Consolas evidence is 13. Space 4/8/12/16/24/32/48/64; radii 0/2/6/10/999; 1 px essential boundary, 4 px selection, 3 px focus+2 offset; Lucide 16/20/24 at 1.75 stroke; ≥44 px targets/8 px gaps. Use a 216 px desktop rail, 64 px bar, 12 columns, breakpoints 375/768/1024/1440, and semantic-first mobile stacking. Mobile DOM/tab order is skip/project controls → main → visually bottom-fixed nav; reserve safe-area padding. Motion 100/150/220/300 ms plus 40 ms stagger, transform/opacity only, reduced motion immediate. Do not import a visual theme. Use current Motion, Radix, Lucide, and native semantic elements first. 21st.dev/shadcn references are behavioral only; magic MCP was unavailable.

Every screen must implement exact meaningful default/loading/empty/error/blocked/offline/recovery states. Preserve exact recovery labels: Save draft and leave/Leave without saving/Stay; Replace file/Remove attachment; Cancel generation/Retry generation/Edit brief; Stop compiling; Retry commit/Save action draft/Discard preview; Retry version/Open metadata only/Return to Compare; Sign in as invited email/Request access from sender/Close review. Generated plates permanently show `Generated presentation · not spatial or buildability evidence`.

## Verification and visual QA

Before handing off, run formatting/lint/typecheck/unit tests/build and focused proof/receipt/provider tests. Add tests for task gating, upload validation, route state/focus restoration, attachment copy consistency, authority structural cues, reduced motion, Canvas fallback, and generated labels. F2.R tests must assert six row states, three relationship summaries, solver disabled until all pass, literal five-base order, full receipt hash wrapping, `.28` ghost, exact camera/control values, reset clearing attestation, and exhaustive pre-B non-disclosure.

Run production Playwright at 1440, 1024, 768, and 375 px plus phone landscape. Test keyboard-only, 200% zoom, reduced motion, offline mode, forced Canvas failure, failed provider/commit/upload/version/permission paths, bottom-nav safe area and tab order. Capture screenshots of F1.2 active/error/mobile, F2.1 options, F2.2 blocked/preview, F2.R Pass A/authority proof/Pass B/receipt/Canvas fallback, F3.1 compare, F3.3 attachment error, and F3.4 identity mismatch. Inspect actual renders for overflow, clipped focus, low-contrast boundaries, unexpected generic cards, early answer leakage, or false status. Report exact commands, test counts, screenshots, final SHA, remaining horizon gaps, and any weak spots honestly.
